[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["vpn-gui-app"]

[tool.ruff]
line-length = 120
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "B"]

[tool.mypy]
python_version = "3.10"
files = ["vpn-gui-app"]
ignore_missing_imports = true
check_untyped_defs = true

